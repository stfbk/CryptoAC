package eu.fbk.st.cryptoac.core.elements

// TODO check this out
actual fun digest(algorithm: String, bytes: ByteArray): String {
    /*val buffer = js("crypto.subtle.digest(algorithm, bytes);")
    return buffer.toString()*/
    return "not me"
}